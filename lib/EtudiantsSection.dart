import 'package:flutter/material.dart';

const color1 = Colors.black;
const color2 = Color(0xFF2ac0a6);
const color3 = Colors.white;

class EtudiantsSection extends StatelessWidget {
  const EtudiantsSection({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(color: color3, child: const Text("EtudiantsSection"));
  }
}
